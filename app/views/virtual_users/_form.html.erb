<%= form_with(model: virtual_user, local: true) do |form| %>
  <% if virtual_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(virtual_user.errors.count, "error") %> prohibited this virtual_user from being saved:</h2>

      <ul>
      <% virtual_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :virtual_domain_id %>
    <%= form.text_field :virtual_domain_id, id: :virtual_user_virtual_domain_id %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password, id: :virtual_user_password %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :virtual_user_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
